const navigationConfig = {
  mainNavLinks: [
    { path: "/", label: "Explore" },
    { path: "/space", label: "Space" },
    { path: "/rent", label: "Rent" },
  ],
};

export default navigationConfig;
